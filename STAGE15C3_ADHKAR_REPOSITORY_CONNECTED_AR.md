# SARI Stage 15C3
تم ربط شاشة الأذكار فعليًا بملف `data/adhkar.json` على iOS وAndroid.
لم تعد نصوص الأذكار نفسها مخزنة داخل ملف الواجهة.
الواجهة تدعم العربية RTL ومعاني اللغات الثماني الأخرى والنطق النصي.
عند فشل قراءة ملف البيانات تظهر حالة خطأ مترجمة بدل انهيار الشاشة.

مهم: محتوى `adhkar.json` ما زال يحمل `review_status=requires_source_verification`.
هذه الدفعة برمجية/تنظيمية ولا تعني اعتماد صحة النصوص أو التكرارات أو الترجمات شرعيًا.

التالي: تدقيق مصادر الأذكار وإضافة حقول source/reference، ثم مواصلة إزالة UI hard-coded strings من بقية التطبيق.
