# SARI iOS v0.9.6 — Build 15 — Release Candidate

هذه النسخة مبنية مباشرة على نسخة v0.9.5 التي ظهرت فيها مشكلة توقف/تعليق تنزيل نموذج الذكاء المحلي ومشكلة خروج التطبيق عند إرسال سؤال إلى «اسأل ساري».

## أهم الإصلاحات

- الإبقاء على **Qwen2.5-3B Instruct Q4_K_M** للمحافظة على جودة الإجابة.
- حماية الاستدلال المحلي بميزانية مدخلات محافظة قبل SwiftLlama، مع `contextSize = 3072` وحد توليد `640` token ومنع الاستدلال المتزامن.
- إزالة إدخال 5 مصادر × 1250 حرفًا بلا حد، واختيار حتى 3 مقاطع مركزة حسب السؤال ضمن الحد الآمن.
- التحقق داخليًا من رموز `[S#]` ثم حذفها من العرض النهائي.
- عرض **«النص المستند إليه»** بعد الإجابة فقط، من دون اسم الكتاب أو المؤلف أو الجزء أو الصفحة أو رابط المصدر.
- حفظ المحادثات محليًا في مجلد مستقل عن حزمة النموذج، بحيث حذف النموذج لا يحذف سجل المحادثات.
- إضافة «محادثة جديدة» وسجل للمحادثات السابقة، من دون إدخال السجل كاملًا في سياق النموذج.
- تحويل تنزيل النموذج إلى `URLSessionConfiguration.background` بمعرّف ثابت، بحيث لا يعتمد التنزيل على بقاء واجهة «اسأل ساري» مفتوحة.
- دعم إعادة ربط مهام التنزيل الخلفية بعد تعليق/إعادة تشغيل التطبيق، والاحتفاظ ببيانات الاستئناف عند أخطاء الشبكة.
- التحقق من SHA-256 قبل تفعيل النموذج.
- إضافة Self-Test لتشغيل النموذج بعد التثبيت قبل إعلان المساعد «جاهز».
- تحرير نموذج الذكاء من الذاكرة عند انتقال التطبيق للخلفية أو عند تحذير الذاكرة، ثم تحميله عند السؤال التالي.
- إضافة سجل تشخيص محلي محدود الحجم لا يسجل نص السؤال أو الإجابة.
- تثبيت إعدادات المشروع على **Swift 6.0 / iOS 17+ / Xcode 16.4**.
- GitHub Actions مبني على `macos-15` ويطلب Xcode 16.4 صراحة، ثم يولّد المشروع بـXcodeGen ويشغّل `xcodebuild` على iPhone generic destination بدون توقيع.

## التحقق المنفذ قبل التغليف

- `python3 scripts/preflight_ios.py` ✅
- `python3 scripts/validate_prayer_math.py` ✅
- `python3 scripts/validate_fiqh_release.py` ✅
- `swiftc -parse` لجميع ملفات Swift في التطبيق والـWidget ✅
- Swift 6 interface type-check لكود LocalFiqhEngine مقابل واجهات SwiftLlama المستخدمة ✅
- Swift 6 structural type-check لـResumableFileDownloader ✅
- فحص ملفات plist / privacy manifests / YAML ✅

## ما يبقى لإعلان النسخة نهائية

هذه البيئة لا تحتوي Xcode/iOS SDK، لذلك يجب أن ينجح GitHub Actions/Xcode 16.4 فعليًا، ثم تُثبّت الـIPA الموقعة على الآيفون ويُختبر تنزيل النموذج والأسئلة القصيرة والمتوسطة والطويلة. نجاح هذه المرحلة هو بوابة الاعتماد النهائي.
