# SARI iOS — Ultimate Fix 0.9.2 (Build 11)

هذه النسخة تجمع الإصلاحات المطلوبة بعد اختبار التطبيق على الآيفون، مع الحفاظ على توافق المشروع مع Xcode 16.4 وSwift 6 وiOS 17+ وGitHub Actions.

## الإصلاحات المنفذة

### 1) المساعد الفقهي المحلي Offline AI
- الاستدلال أصبح محليًا عبر `SwiftLlama 0.1.0` بعد تنزيل نموذج GGUF مرة واحدة على الجهاز.
- النموذج الموصى به: `Qwen2.5-3B-Instruct Q4_K_M`.
- المصادر الفقهية تبقى محلية داخل التطبيق (`fiqh_pages.sqlite3`).
- الأسئلة غير العربية تُحوَّل محليًا إلى استعلام بحث عربي، ثم يُولد الجواب النهائي بلغة التطبيق.
- الجواب مقيد بالمقاطع المسترجعة، ويُصنف إلى: نص صريح / استنتاج من المصدر / مصدر غير كافٍ.
- أي جواب بلا إحالة صحيحة `[S#]` إلى مصدر مسترجع يتم خفضه تلقائيًا إلى «المصدر غير كافٍ» بدل عرضه كحكم موثوق.
- يظهر المصدر والجزء والصفحة والمقطع للمستخدم.

### 2) كيبورد «اسأل ساري»
- يختفي تلقائيًا عند إرسال السؤال.
- يدعم السحب التفاعلي للأسفل عبر `scrollDismissesKeyboard(.interactively)`.
- الضغط على مساحة الإجابة يغلق التركيز عن حقل الإدخال.
- الإجابة تبقى قابلة للتمرير والقراءة ولا يُفترض أن يحجبها الكيبورد بعد الإرسال.

### 3) مواقيت الصلاة
- استبدال مسار الحساب الذي كان يسبب انزياحًا كبيرًا في الأوقات.
- الحساب يعتمد على الإحداثيات + التاريخ المدني + المنطقة الزمنية + طريقة الحساب + مذهب العصر + التعديلات اليدوية.
- الوضع التلقائي يختار طريقة مناسبة للموقع، ومنها أم القرى في السعودية.
- إعادة الحساب عند تغير اليوم، الموقع، المنطقة الزمنية، أو إعدادات الحساب.
- الـWidget وإشعارات الصلاة يستخدمان نفس `PrayerStore` ونفس نتيجة الحساب.
- اختبار الانحدار لمكة المكرمة بتاريخ 2026-09-08 يمر داخل `scripts/validate_prayer_math.py`.

### 4) المصحف
- القارئ لم يعد يعيد تركيب صفحة المصحف من أسطر نصية كطريقة العرض الأساسية.
- تمت إضافة `MushafPageRepository` لعرض صفحات مصحف المدينة (حفص، KFQC) كـSVG دقيق للصفحة.
- الصفحات تُخزن محليًا بعد تحميلها لتعمل لاحقًا من الكاش.
- دعم التنقل بين 604 صفحة والانتقال المباشر لرقم الصفحة.
- عند عدم توفر الصفحة الدقيقة لأول مرة، يظهر النص المحلي مؤقتًا مع زر إعادة المحاولة بدل تشويه التخطيط.

### 5) تغيير اللغة فورًا
- `RootTabView` مرتبط مباشرة بـ `@AppStorage("sariLanguage")` ويُعاد تكوين الواجهة الحالية عند تغير اللغة مع الاحتفاظ بالتبويب الحالي.
- تمرير `Locale` و`layoutDirection` من مستوى الجذر.
- تحديث اسم الموقع والـWidget بعد تغيير اللغة.
- اللغات المدعومة: العربية، الإنجليزية، التركية، الملايوية، الإندونيسية، اليابانية، الصينية، الروسية، الفرنسية.

## توافق البناء
- Xcode: 16.4 مستهدف في GitHub Actions عند توفره على runner.
- Swift: 6.0
- iOS Deployment Target: 17.0
- XcodeGen: 2.45.4+
- SwiftLlama: 0.1.0 مثبت بإصدار exact.
- App version: 0.9.2 (11)

## الفحوص المنفذة محليًا
- `python3 scripts/preflight_ios.py` ✅
- `python3 scripts/validate_prayer_math.py` ✅
- `swiftc -parse` لكل ملفات Swift في التطبيق والWidget ✅
- جميع ملفات JSON (51 ملفًا) قابلة للقراءة ✅
- لا يوجد تصادم `scholars.json` ✅
- يوجد ملف `scholars.json` واحد فقط داخل `SARI/Resources/data/` ✅

## الفحص النهائي المطلوب
الفحص الذي لا يمكن تنفيذه على Windows/Linux هو ربط المشروع كاملًا مع iOS SDK وSwiftUI/WebKit/WidgetKit وSwiftLlama. لذلك ملف GitHub Actions المرفق يقوم بـ:
1. preflight
2. Swift parse
3. XcodeGen
4. resolve Swift package
5. `xcodebuild` لجهاز iOS بدون signing
6. فحص التطبيق والWidget والموارد
7. إنشاء `SARI-unsigned.ipa`

إذا ظهر اللون الأخضر في GitHub Actions بعد رفع هذه النسخة، فهذا هو اختبار البناء الفعلي على macOS/Xcode.
