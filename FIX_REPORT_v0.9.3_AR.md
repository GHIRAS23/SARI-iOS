# SARI iOS v0.9.3 — إصلاح تنزيل النموذج والأذان

## 1) تنزيل نموذج Qwen المحلي

تم استبدال التنزيل المباشر البسيط للملف الكبير بمدير تنزيل مخصص للملفات الكبيرة:

- حفظ Resume Data عند انقطاع الشبكة أو انتهاء المهلة.
- إعادة المحاولة تلقائيًا حتى 3 مرات مع محاولة الاستكمال بدل البدء من الصفر.
- مهلة مورد ممتدة إلى 6 ساعات و`waitsForConnectivity` مفعّل.
- حفظ النموذج في `model.gguf.part` إلى أن يكتمل التنزيل والتحقق.
- التحقق المتدرج من SHA-256 بدون تحميل ملف 2.1 GB كاملًا إلى RAM.
- عدم تفعيل النموذج إلا بعد نجاح SHA-256.
- إظهار الحجم المحمّل / الإجمالي / النسبة / سرعة التنزيل في واجهة الإعداد.
- الاحتفاظ ببيانات الاستئناف عند فشل الشبكة بدل حذف كل شيء.
- رسائل خطأ أكثر تحديدًا للاتصال، نقص المساحة، وفشل سلامة الملف.

> ملاحظة: الاستئناف يعتمد على Resume Data التي يوفرها iOS. إذا أبطل النظام بيانات الاستئناف نفسها، يبدأ التنزيل من جديد كخيار أخير بدل اعتماد ملف ناقص.

## 2) فحص وإصلاح الأذان

تم فحص ملفات الأذان الأربعة الفعلية في الحزمة. جميع ملفات CAF:

| الملف | الترميز | المدة |
|---|---|---:|
| AdhanDughariri.caf | LPCM / 22050 Hz / mono | 16.67 s |
| AdhanAliMulla.caf | LPCM / 22050 Hz / mono | 20.39 s |
| AdhanQatami.caf | LPCM / 22050 Hz / mono | 23.92 s |
| AdhanBaafif.caf | LPCM / 22050 Hz / mono | 25.61 s |

كلها أقل من 30 ثانية ومناسبة كأصوات إشعارات iOS، كما أن تحليل الصوت أكد أنها ليست ملفات صامتة.

### الخلل البرمجي الذي تم إصلاحه

لم يكن التطبيق يعيّن `UNUserNotificationCenterDelegate`، وبالتالي عند اختبار تنبيه الصلاة بينما SARI مفتوح في الواجهة الأمامية يمكن لـ iOS منع عرض الإشعار وصوته. أضيف الآن delegate يعرض الإشعار والصوت في foreground.

كما تم:

- التأكد وقت التشغيل أن ملف CAF المختار موجود فعلًا في `Bundle.main` قبل استخدامه.
- الرجوع إلى صوت النظام بدل جدولة مسار صوت مفقود.
- إضافة زر **اختبار صوت الأذان** يرسل تنبيهًا تجريبيًا بعد 3 ثوانٍ باستخدام نفس مسار الصوت المستخدم في الصلاة الحقيقية.
- إضافة فحص Release Gate في `scripts/preflight_ios.py` يتحقق من CAF وأن مدة كل صوت أقل من 30 ثانية، إضافة إلى وجود ملفات M4A للمعاينة.

## 3) الإصدار

- Marketing Version: `0.9.3`
- Build: `12`

## 4) اختبارات ثابتة أجريت على الحزمة

- Swift syntax parse: PASS
- Python preflight syntax: PASS
- SARI iOS preflight: PASS
- لا توجد resource basename collisions: PASS
- JSON / SQLite / plist validation: PASS
- Adhan CAF/M4A asset validation: PASS
